# AGENTS.md

## 開発方針

このプロジェクトは、フロントエンド担当とバックエンド担当を分けて開発する。

- フロントエンド担当者の明示的な合意なしに、画面仕様、表示項目、画面挙動、フロント側が前提としているデータ構造を変更しないこと。
- バックエンド担当者の明示的な合意なしに、API、レスポンススキーマ、リクエスト形式、フィールド名、データ契約を変更しないこと。
- API契約はフロントエンドとバックエンドの共有インターフェースとして扱うこと。破壊的変更や挙動変更を伴う更新は、実装前に必ず協議と合意を行うこと。
- 片側の変更によってもう片側の変更が必要になる場合は、黙って両方を変更せず、先に契約変更内容を明文化して合意を取ること。
- 変更が必要な場合は、可能な限り後方互換性を保つ追加方式を優先すること。

## 作業ルール

- フロントエンドを作業する場合、明示的に承認されたAPI契約変更の指示がない限り、バックエンドの仕様は固定とみなすこと。
- バックエンドを作業する場合、明示的に承認されたAPI契約変更の指示がない限り、フロントエンドの期待仕様は固定とみなすこと。
- 現在の実装と期待される契約のどちらが正なのか不明な場合は、コードを変更する前に契約内容を確認すること。
